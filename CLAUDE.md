@AGENTS.md
@memory.md

# Claude Code

Deep context in `.agents/` -- profiles, skills, workflows, contexts.
Read `experiment.json` `"profile"` field to find the right `.agents/profiles/` doc.
For library-specific patterns, read `.agents/skills/<name>/SKILL.md`.
Cursor-native content tools: `.cursor/rules/`, `.cursor/skills/`, `.cursor/agents/`.
Content system overview: `.agents/contexts/content-constellation.md`.
